{
    "name": "priem-pkgod",
    "version": "1.0.0",
    "description": "Приемная комиссия - система создания расписок",
    "scripts": {
      "dev": "netlify dev",
      "build": "netlify build",
      "deploy": "netlify deploy --prod",
      "env:set": "netlify env:import .env"
    },
    "dependencies": {
      "@supabase/supabase-js": "^2.39.0"
    },
    "devDependencies": {
      "netlify-cli": "^17.0.0"
    }
  }